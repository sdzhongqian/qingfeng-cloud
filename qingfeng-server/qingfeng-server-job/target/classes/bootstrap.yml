spring:
  application:
    name: qingfeng-Server-Job
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8001
      config:
        server-addr: localhost:8001
        group: DEFAULT_GROUP
        prefix: qingfeng-server-job
        file-extension: yaml

logging:
  level:
    com:
      alibaba:
        cloud:
          nacos:
            client:
              NacosPropertySourceBuilder: error